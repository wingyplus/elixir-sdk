# Dagger.Codegen

This packages provides a tool to generate Elixir code Dagger GraphQL.

## How to run

```bash
mix deps.get
mix dagger.codegen generate --outdir <dir> --introspection <introspection.json>
```
